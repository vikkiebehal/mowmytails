<section class="mt-section mt-section--soft"><div class="mt-page"><div class="mt-section-head"><span class="mt-eyebrow">Complete their routine</span><h2 class="mt-title">You May Also Love</h2></div><div class="mt-product-grid">{% for product in recommendations.products limit: 4 %}{% render 'product-card', product: product, show_rating: true %}{% else %}{% for product in collections.all.products limit: 4 %}{% render 'product-card', product: product, show_rating: true %}{% endfor %}{% endfor %}</div></div></section>
{% schema %}{"name":"Product recommendations","settings":[]} {% endschema %}
